name: Update profiles.dev

on:
  push:
    branches: [main]
    paths:
      - profile.yaml

permissions:
  id-token: write
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Update profiles.dev
        uses: nomadops/profiles.dev@v2
