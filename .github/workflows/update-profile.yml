name: Update profiles.dev

on:
  push:
    branches: [main]
    paths:
      - profile.yaml

permissions:
  id-token: write
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Update profiles.dev
        uses: nomadops/profiles.dev@3233e2e5c2e8f75b8e9bf3991c4fb778ad020cf4
